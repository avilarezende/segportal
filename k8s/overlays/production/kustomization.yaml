apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: registry.aqne.jus.br/segportal/guacamole
    newTag: latest
  - name: registry.aqne.jus.br/segportal/guacd
    newTag: latest
  - name: registry.aqne.jus.br/segportal/egress-proxy
    newTag: latest
  - name: registry.aqne.jus.br/segportal/web-browser
    newTag: latest

configMapGenerator:
  - name: segportal-common
    behavior: merge
    literals:
      - GUACAMOLE_HOSTNAME=segportal.aqne.jus.br
      - SESSION_TIMEOUT_MINUTES=60
      - MFA_ENABLED=true

patches:
  - target:
      kind: Deployment
      name: guacamole
    patch: |
      - op: replace
        path: /spec/replicas
        value: 3
  - target:
      kind: Deployment
      name: guacd
    patch: |
      - op: replace
        path: /spec/replicas
        value: 3

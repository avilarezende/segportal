# SegPortal TJSE — Squid egress proxy (reference config for Docker Compose)
# Production uses services/egress-proxy/squid.conf baked into the image.

acl localnet src 10.0.0.0/8
acl localnet src 172.16.0.0/12
acl localnet src 192.168.0.0/16
acl localnet src fc00::/7
acl localnet src fe80::/10

acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 443
acl CONNECT method CONNECT

acl tjse_whitelist dstdomain .tjse.jus.br
acl tjse_whitelist dstdomain .jus.br
acl tjse_whitelist dstdomain .gov.br
acl tjse_whitelist dstdomain .pje.jus.br

http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
http_access allow localhost manager
http_access deny manager
http_access allow localnet tjse_whitelist
http_access deny all

http_port 3128

coredump_dir /var/spool/squid
access_log stdio:/var/log/squid/access.log squid
cache_log /var/log/squid/cache.log

# Forward with institutional egress IP (set at infrastructure layer)
forwarded_for off
via off
